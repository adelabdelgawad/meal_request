/**
 * Home page - Server Component
 *
 * Currently empty - will contain the main application purpose in the future.
 * Authentication is handled by the (pages)/layout.tsx via AppNavigationServer.
 */
export default function Home() {
  return (
    <main className="container mx-auto p-8">
      {/* Main application content will go here */}
    </main>
  );
}
