# ==============================================================================
# Frontend Application Configuration (Next.js) - TEMPLATE
# ==============================================================================
# Copy this file to .env.frontend and update with your values
# ==============================================================================

# ==============================================================================
# Node Environment
# ==============================================================================
NODE_ENV=production  # Options: development, production

# ==============================================================================
# Backend API Configuration
# ==============================================================================
# Server-side API URL (used by Next.js API routes to connect to backend)
# Use Docker service name for container-to-container communication
BACKEND_API_URL=http://backend:8000  # Use localhost:8000 for local dev

# API version
API_VERSION=v1

# ==============================================================================
# Site URL Configuration
# ==============================================================================
# Site URL for SSR internal calls (Next.js calling its own API routes)
# Use container name for Docker network communication
SITE_URL=http://frontend:3000  # Use localhost:3000 for local dev

# ==============================================================================
# Client-Side Configuration
# ==============================================================================
# Client-side API URL (exposed to browser)
# Uses Next.js API routes as proxy, so path is relative
NEXT_PUBLIC_API_URL=/api

# Session management flag
NEXT_PUBLIC_USE_STATEFUL_SESSIONS=true

# ==============================================================================
# Session Cookie Configuration
# ==============================================================================
# Session cookie name (must match backend SESSION_COOKIE_NAME)
SESSION_COOKIE_NAME=refresh

# ==============================================================================
# Data Polling Intervals (milliseconds)
# ==============================================================================
# How often to refresh data on client-side
NEXT_PUBLIC_REQUESTS_POLL_INTERVAL=30000      # 30 seconds
NEXT_PUBLIC_ANALYSIS_POLL_INTERVAL=60000      # 1 minute
NEXT_PUBLIC_HISTORY_POLL_INTERVAL=60000       # 1 minute
NEXT_PUBLIC_SCHEDULER_POLL_INTERVAL=3000      # 3 seconds

# ==============================================================================
# Development Origins
# ==============================================================================
# Allowed dev origins for cross-origin requests (comma-separated)
# Add network IPs for development access from other devices
ALLOWED_DEV_ORIGINS=192.168.1.195,192.168.1.100
